---
import Icon from 'astro-icon'
import Button from '~components/ui/Button.astro'
import {openCollectiveProducts} from '~constants/products'
---

<div class='flex flex-col lg:flex-row justify-center lg:justify-between gap-4 lg:gap-6'>
  {
    openCollectiveProducts.map((product) => (
      <div class='bg-zinc-800 w-full p-5 lg:p-8 rounded-3xl flex flex-col gap-2.5 lg:gap-5 lg:text-lg'>
        <div class='flex items-center justify-center w-10 h-10 lg:w-16 lg:h-16 bg-gradient-primary rounded-full'>
          <Icon name={product.icon} class='w-6 h-6 lg:w-10 lg:h-10 text-white' />
        </div>
        <div class='text-lg lg:text-2xl font-semibold leading-7 pt-2.5'>{product.title}</div>
        <div class='font-medium leading-[60px] text-[40px] lg:text-[54px]'>€{product.price}</div>
        <p class='pb-1.5 lg:pb-2 text-gray-dark'>{product.description}</p>
        <div class='flex gap-3 items-center mt-auto'>
          <Icon name='check' class='w-6 h-6' />
          <div class='font-medium leading-relaxed'>1 pack for you</div>
        </div>
        <div class='flex gap-3 pb-3.5'>
          <Icon name='check' class='w-6 h-6 lg:w-7 lg:h-7' />
          <div class='font-medium leading-relaxed'>
            {product.quantity - 1} pack{product.quantity === 2 ? '' : 's'} for gifting
          </div>
        </div>
        <Button href={product.url} tag='a' target='_blank' full>
          Pre-Order
        </Button>
      </div>
    ))
  }
</div>
