export const AddressForm = () => {
  // validate referrer
  console.log(document.referrer)
  // validate query params
  // query exits
  // validate purchase id in db
  // handle invalid referral and query params
  // render form
  return (
    <form id='address-form'>
      <h3>Address</h3>
      <div id='address-element'></div>
    </form>
  )
}
