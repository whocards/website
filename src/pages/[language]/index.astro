---
import LangingPage from '~components/LandingPage.astro'
import {LANGUAGES} from '~/utils'

export async function getStaticPaths() {
  return Object.entries(LANGUAGES).map(([language, name]) => ({
    params: {language},
    props: {name},
  }))
}
---

<LangingPage />
